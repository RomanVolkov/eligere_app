name: Build and Release


permissions:
  contents: write

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Build DMG
      run: ./scripts/build_dmg.sh

    - name: Get DMG Version
      id: get_version
      run: |
        DMG_NAME=$(ls ${{ github.workspace }}/output/Eligere_*_Installer_compressed.dmg)
        echo "DMG_NAME=${DMG_NAME}" >> $GITHUB_ENV

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        files: ${{ env.DMG_NAME }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
